# BUG-137: applyBlendModeToGroup() Crashes on Null Group

## Summary
applyBlendModeToGroup() has no null check on the `group` parameter. Passing null/undefined causes immediate TypeError crash at line 126.

## Severity: LOW

## Location
- File: /ui/src/engine/layers/blendModeUtils.ts
- Lines: 125-126
- Function: applyBlendModeToGroup()

## Crash Path Trace
```
Function: applyBlendModeToGroup(group: THREE.Group, mode: string)

Line 125: export function applyBlendModeToGroup(group: THREE.Group, mode: string): void {
  - No validation on group parameter

Line 126: group.traverse((child) => {
  - group=null → TypeError: Cannot read properties of null (reading 'traverse')
  - group=undefined → TypeError: Cannot read properties of undefined (reading 'traverse')

Result: CRASH - TypeError
```

## Code Evidence
```typescript
// Line 125-133
export function applyBlendModeToGroup(group: THREE.Group, mode: string): void {
  group.traverse((child) => {  // CRASH HERE if group is null
    if (child instanceof THREE.Mesh && child.material) {
      const material = child.material as THREE.Material;
      setMaterialBlendMode(material, mode);
      material.needsUpdate = true;
    }
  });
}
```

## Impact
- Unhandled crash if caller passes null group
- Stack trace points to blendModeUtils.ts, not caller's error
- No defensive coding

## Suggested Fix
```typescript
export function applyBlendModeToGroup(group: THREE.Group, mode: string): void {
  if (!group) {
    console.warn('applyBlendModeToGroup: group is null/undefined');
    return;
  }
  group.traverse((child) => {
    // ... rest of function
  });
}
```
