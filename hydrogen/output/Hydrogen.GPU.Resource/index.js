// Generated by purs version 0.15.15
import * as Control_Bind from "../Control.Bind/index.js";
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_List_Types from "../Data.List.Types/index.js";
import * as Data_Map_Internal from "../Data.Map.Internal/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Set from "../Data.Set/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Data_Tuple from "../Data.Tuple/index.js";
import * as Data_Unfoldable from "../Data.Unfoldable/index.js";
import * as Hydrogen_GPU_WebGPU_Types from "../Hydrogen.GPU.WebGPU.Types/index.js";
var eq1 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_GPU_WebGPU_Types.eqGPUTextureFormat);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showString);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var toUnfoldable = /* #__PURE__ */ Data_Map_Internal.toUnfoldable(Data_Unfoldable.unfoldableArray);
var map1 = /* #__PURE__ */ Data_Functor.map(Data_Map_Internal.functorMap);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordString);
var eq3 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Eq.eqArray(Hydrogen_GPU_WebGPU_Types.eqGPUTextureUsage));
var foldl1 = /* #__PURE__ */ Data_Foldable.foldl(Data_List_Types.foldableList);
var bind = /* #__PURE__ */ Control_Bind.bind(Data_Maybe.bindMaybe);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var Dim2D = /* #__PURE__ */ (function () {
    function Dim2D() {

    };
    Dim2D.value = new Dim2D();
    return Dim2D;
})();
var Dim3D = /* #__PURE__ */ (function () {
    function Dim3D() {

    };
    Dim3D.value = new Dim3D();
    return Dim3D;
})();
var DimCube = /* #__PURE__ */ (function () {
    function DimCube() {

    };
    DimCube.value = new DimCube();
    return DimCube;
})();
var ResourceId = function (x) {
    return x;
};
var ResourceHandle = function (x) {
    return x;
};
var TextureHandle = function (x) {
    return x;
};
var TexturePool = function (x) {
    return x;
};
var PipelineKey = function (x) {
    return x;
};
var PipelineCache = function (x) {
    return x;
};
var Bucket1KB = /* #__PURE__ */ (function () {
    function Bucket1KB() {

    };
    Bucket1KB.value = new Bucket1KB();
    return Bucket1KB;
})();
var Bucket4KB = /* #__PURE__ */ (function () {
    function Bucket4KB() {

    };
    Bucket4KB.value = new Bucket4KB();
    return Bucket4KB;
})();
var Bucket16KB = /* #__PURE__ */ (function () {
    function Bucket16KB() {

    };
    Bucket16KB.value = new Bucket16KB();
    return Bucket16KB;
})();
var Bucket64KB = /* #__PURE__ */ (function () {
    function Bucket64KB() {

    };
    Bucket64KB.value = new Bucket64KB();
    return Bucket64KB;
})();
var Bucket256KB = /* #__PURE__ */ (function () {
    function Bucket256KB() {

    };
    Bucket256KB.value = new Bucket256KB();
    return Bucket256KB;
})();
var Bucket1MB = /* #__PURE__ */ (function () {
    function Bucket1MB() {

    };
    Bucket1MB.value = new Bucket1MB();
    return Bucket1MB;
})();
var Bucket4MB = /* #__PURE__ */ (function () {
    function Bucket4MB() {

    };
    Bucket4MB.value = new Bucket4MB();
    return Bucket4MB;
})();
var Bucket16MB = /* #__PURE__ */ (function () {
    function Bucket16MB() {

    };
    Bucket16MB.value = new Bucket16MB();
    return Bucket16MB;
})();
var BucketCustom = /* #__PURE__ */ (function () {
    function BucketCustom(value0) {
        this.value0 = value0;
    };
    BucketCustom.create = function (value0) {
        return new BucketCustom(value0);
    };
    return BucketCustom;
})();
var BufferHandle = function (x) {
    return x;
};
var BufferPool = function (x) {
    return x;
};
var ResourceRegistry = function (x) {
    return x;
};
var withTextures = function (f) {
    return function (v) {
        return {
            pipelines: v.pipelines,
            buffers: v.buffers,
            currentFrame: v.currentFrame,
            gcThreshold: v.gcThreshold,
            textures: f(v.textures)
        };
    };
};
var withPipelines = function (f) {
    return function (v) {
        return {
            buffers: v.buffers,
            textures: v.textures,
            currentFrame: v.currentFrame,
            gcThreshold: v.gcThreshold,
            pipelines: f(v.pipelines)
        };
    };
};
var withBuffers = function (f) {
    return function (v) {
        return {
            pipelines: v.pipelines,
            textures: v.textures,
            currentFrame: v.currentFrame,
            gcThreshold: v.gcThreshold,
            buffers: f(v.buffers)
        };
    };
};
var texturePoolStats = function (v) {
    var totalReused = v.totalAcquired - v.totalAllocated | 0;
    var reuseRate = (function () {
        var $240 = v.totalAcquired > 0;
        if ($240) {
            return Data_Int.toNumber(totalReused) / Data_Int.toNumber(v.totalAcquired);
        };
        return 0.0;
    })();
    var freeCount = Data_Array.length(v.freeTextures);
    var acquiredCount = Data_Set.size(v.acquired);
    return {
        freeCount: freeCount,
        acquiredCount: acquiredCount,
        totalAllocated: v.totalAllocated,
        reuseRate: reuseRate
    };
};
var textureMatches = function (spec) {
    return function (v) {
        return v.spec.width === spec.width && (v.spec.height === spec.height && (v.spec.depth === spec.depth && (eq1(v.spec.format)(spec.format) && v.spec.mipLevels >= spec.mipLevels)));
    };
};
var showTexturePool = {
    show: function (v) {
        return "(TexturePool allocated:" + (show(v.totalAllocated) + (" acquired:" + (show(Data_Set.size(v.acquired)) + ")")));
    }
};
var showTextureHandle = {
    show: function (v) {
        return "(TextureHandle " + (show(v.spec.width) + ("x" + (show(v.spec.height) + ")")));
    }
};
var showTextureDimension = {
    show: function (v) {
        if (v instanceof Dim2D) {
            return "2D";
        };
        if (v instanceof Dim3D) {
            return "3D";
        };
        if (v instanceof DimCube) {
            return "Cube";
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 668, column 1 - line 671, column 24): " + [ v.constructor.name ]);
    }
};
var showResourceRegistry = {
    show: function (v) {
        return "(ResourceRegistry frame:" + (show(v.currentFrame) + ")");
    }
};
var showResourceId = {
    show: function (v) {
        return "(ResourceId " + (show1(v) + ")");
    }
};
var show2 = /* #__PURE__ */ Data_Show.show(showResourceId);
var showResourceHandle = {
    show: function (v) {
        return "(ResourceHandle " + (show2(v.id) + (" gen:" + (show(v.generation) + ")")));
    }
};
var showPipelineKey = {
    show: function (v) {
        return "(PipelineKey " + (v + ")");
    }
};
var showPipelineCache = {
    show: function (v) {
        return "(PipelineCache entries:" + (show(Data_Map_Internal.size(v.entries)) + ("/" + (show(v.maxEntries) + ")")));
    }
};
var showBufferPool = {
    show: function (v) {
        return "(BufferPool allocated:" + (show(v.totalAllocated) + (" acquired:" + (show(Data_Set.size(v.acquired)) + ")")));
    }
};
var showBufferBucket = {
    show: function (v) {
        if (v instanceof Bucket1KB) {
            return "Bucket1KB";
        };
        if (v instanceof Bucket4KB) {
            return "Bucket4KB";
        };
        if (v instanceof Bucket16KB) {
            return "Bucket16KB";
        };
        if (v instanceof Bucket64KB) {
            return "Bucket64KB";
        };
        if (v instanceof Bucket256KB) {
            return "Bucket256KB";
        };
        if (v instanceof Bucket1MB) {
            return "Bucket1MB";
        };
        if (v instanceof Bucket4MB) {
            return "Bucket4MB";
        };
        if (v instanceof Bucket16MB) {
            return "Bucket16MB";
        };
        if (v instanceof BucketCustom) {
            return "(BucketCustom " + (show(v.value0) + ")");
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 472, column 1 - line 481, column 60): " + [ v.constructor.name ]);
    }
};
var show3 = /* #__PURE__ */ Data_Show.show(showBufferBucket);
var showBufferHandle = {
    show: function (v) {
        return "(BufferHandle " + (show3(v.bucket) + (" size:" + (show(v.actualSize) + ")")));
    }
};
var resourceIdToString = function (v) {
    return v;
};
var registryTextures = function (v) {
    return v.textures;
};
var registryPipelines = function (v) {
    return v.pipelines;
};
var registryFrame = function (v) {
    return v.currentFrame;
};
var registryBuffers = function (v) {
    return v.buffers;
};
var recentPipelines = function (withinFrames) {
    return function (v) {
        var threshold = v.currentFrame - withinFrames | 0;
        var allEntries = map(function (v1) {
            return v1.value1;
        })(toUnfoldable(v.entries));
        return Data_Array.filter(function (e) {
            return e.lastUsedFrame >= threshold;
        })(allEntries);
    };
};
var pipelineKeys = function (v) {
    return map(function (v1) {
        return v1.value0;
    })(toUnfoldable(v.entries));
};
var pipelineHasCapacity = function (v) {
    return Data_Map_Internal.size(v.entries) < v.maxEntries;
};
var pipelineCacheStats = function (v) {
    var total = v.hits + v.misses | 0;
    var hitRate = (function () {
        var $271 = total > 0;
        if ($271) {
            return Data_Int.toNumber(v.hits) / Data_Int.toNumber(total);
        };
        return 0.0;
    })();
    return {
        entries: Data_Map_Internal.size(v.entries),
        maxEntries: v.maxEntries,
        hits: v.hits,
        misses: v.misses,
        hitRate: hitRate
    };
};
var pipelineCacheSize = function (v) {
    return Data_Map_Internal.size(v.entries);
};
var mkTextureSpec = function (width) {
    return function (height) {
        return function (format) {
            return function (usage) {
                return function (label) {
                    return {
                        width: width,
                        height: height,
                        depth: 1,
                        format: format,
                        usage: usage,
                        mipLevels: 1,
                        dimension: Dim2D.value,
                        label: label
                    };
                };
            };
        };
    };
};
var mkTexturePool = {
    freeTextures: [  ],
    acquired: Data_Set.empty,
    totalAllocated: 0,
    totalAcquired: 0,
    totalReleased: 0
};
var mkResourceId = ResourceId;
var mkResourceHandle = function (contentKey) {
    return {
        id: mkResourceId(contentKey),
        generation: 0
    };
};
var mkPipelineKey = PipelineKey;
var mkPipelineCache = function (maxEntries) {
    return {
        entries: Data_Map_Internal.empty,
        maxEntries: maxEntries,
        currentFrame: 0,
        hits: 0,
        misses: 0
    };
};
var mkBufferSpec = function (size) {
    return function (usage) {
        return function (label) {
            return {
                size: size,
                usage: usage,
                label: label
            };
        };
    };
};
var mkBufferPool = {
    freeLists: Data_Map_Internal.empty,
    acquired: Data_Set.empty,
    totalAllocated: 0,
    totalAcquired: 0,
    totalReleased: 0
};
var mapFreeTextures = function (f) {
    return function (v) {
        var newFreeTextures = map(f)(v.freeTextures);
        return {
            acquired: v.acquired,
            totalAllocated: v.totalAllocated,
            totalAcquired: v.totalAcquired,
            totalReleased: v.totalReleased,
            freeTextures: newFreeTextures
        };
    };
};
var mapFreeBuffers = function (f) {
    return function (v) {
        var newFreeLists = map1(map(f))(v.freeLists);
        return {
            acquired: v.acquired,
            totalAllocated: v.totalAllocated,
            totalAcquired: v.totalAcquired,
            totalReleased: v.totalReleased,
            freeLists: newFreeLists
        };
    };
};
var largeTextures = function (minWidth) {
    return function (minHeight) {
        return function (v) {
            return Data_Array.filter(function (v1) {
                return v1.spec.width > minWidth && v1.spec.height > minHeight;
            })(v.freeTextures);
        };
    };
};
var incrementGeneration = function (v) {
    return {
        id: v.id,
        generation: v.generation + 1 | 0
    };
};
var handleId = function (v) {
    return v.id;
};
var handleGeneration = function (v) {
    return v.generation;
};
var findSmallestFittingTexture = function (width) {
    return function (height) {
        return function (v) {
            var fitting = Data_Array.filter(function (v1) {
                return v1.spec.width >= width && v1.spec.height >= height;
            })(v.freeTextures);
            var sorted = Data_Array.sortBy(function (v1) {
                return function (v2) {
                    return compare(v1.spec.width * v1.spec.height | 0)(v2.spec.width * v2.spec.height | 0);
                };
            })(fitting);
            return Data_Array.head(sorted);
        };
    };
};
var expensivePipelines = function (thresholdMs) {
    return function (v) {
        var allEntries = map(function (v1) {
            return v1.value1;
        })(toUnfoldable(v.entries));
        return Data_Array.filter(function (e) {
            return e.compilationTime > thresholdMs;
        })(allEntries);
    };
};
var estimateTextureMemory = function (spec) {
    var bytesPerPixel = function (v) {
        return 4;
    };
    var mipFactor = (function () {
        var $295 = spec.mipLevels > 1;
        if ($295) {
            return 4.0 / 3.0;
        };
        return 1.0;
    })();
    var baseSize = ((spec.width * spec.height | 0) * spec.depth | 0) * bytesPerPixel(spec.format) | 0;
    var mipMultiplied = Data_Int.toNumber(baseSize) * mipFactor;
    return Data_Int.floor(mipMultiplied);
};
var texturePoolMemoryUsed = function (v) {
    return foldl(function (acc) {
        return function (v1) {
            return acc + estimateTextureMemory(v1.spec) | 0;
        };
    })(0)(v.freeTextures);
};
var texturePoolUnderThreshold = function (thresholdBytes) {
    return function (pool) {
        return texturePoolMemoryUsed(pool) < thresholdBytes;
    };
};
var eqTextureDimension = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Dim2D && y instanceof Dim2D) {
                return true;
            };
            if (x instanceof Dim3D && y instanceof Dim3D) {
                return true;
            };
            if (x instanceof DimCube && y instanceof DimCube) {
                return true;
            };
            return false;
        };
    }
};
var eq4 = /* #__PURE__ */ Data_Eq.eq(eqTextureDimension);
var ordTextureDimension = {
    compare: function (x) {
        return function (y) {
            if (x instanceof Dim2D && y instanceof Dim2D) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Dim2D) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Dim2D) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Dim3D && y instanceof Dim3D) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Dim3D) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Dim3D) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof DimCube && y instanceof DimCube) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqTextureDimension;
    }
};
var eqResourceId = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var eq5 = /* #__PURE__ */ Data_Eq.eq(eqResourceId);
var eq6 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Set.eqSet(eqResourceId));
var ordResourceId = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqResourceId;
    }
};
var $$delete = /* #__PURE__ */ Data_Set["delete"](ordResourceId);
var insert = /* #__PURE__ */ Data_Set.insert(ordResourceId);
var compare2 = /* #__PURE__ */ Data_Ord.compare(/* #__PURE__ */ Data_Tuple.ordTuple(ordResourceId)(Data_Ord.ordInt));
var textureRelease = function (v) {
    return function (v1) {
        var resourceId = handleId(v.handle);
        var newFreeTextures = Data_Array.cons(v)(v1.freeTextures);
        var newAcquired = $$delete(resourceId)(v1.acquired);
        return {
            totalAllocated: v1.totalAllocated,
            totalAcquired: v1.totalAcquired,
            freeTextures: newFreeTextures,
            acquired: newAcquired,
            totalReleased: v1.totalReleased + 1 | 0
        };
    };
};
var eqResourceHandle = {
    eq: function (x) {
        return function (y) {
            return x.generation === y.generation && eq5(x.id)(y.id);
        };
    }
};
var eq7 = /* #__PURE__ */ Data_Eq.eq(eqResourceHandle);
var eqTextureHandle = {
    eq: function (x) {
        return function (y) {
            return eq7(x.handle)(y.handle) && (x.spec.depth === y.spec.depth && eq4(x.spec.dimension)(y.spec.dimension) && eq1(x.spec.format)(y.spec.format) && x.spec.height === y.spec.height && x.spec.label === y.spec.label && x.spec.mipLevels === y.spec.mipLevels && eq3(x.spec.usage)(y.spec.usage) && x.spec.width === y.spec.width);
        };
    }
};
var eq8 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Eq.eqArray(eqTextureHandle));
var notEq = /* #__PURE__ */ Data_Eq.notEq(eqTextureHandle);
var eqTexturePool = {
    eq: function (x) {
        return function (y) {
            return eq6(x.acquired)(y.acquired) && eq8(x.freeTextures)(y.freeTextures) && x.totalAcquired === y.totalAcquired && x.totalAllocated === y.totalAllocated && x.totalReleased === y.totalReleased;
        };
    }
};
var eq9 = /* #__PURE__ */ Data_Eq.eq(eqTexturePool);
var textureAcquire = function (spec) {
    return function (v) {
        var unwrapTextureHandle = function (v1) {
            return v1.handle;
        };
        var matchingTexture = Data_Array.find(textureMatches(spec))(v.freeTextures);
        if (matchingTexture instanceof Data_Maybe.Just) {
            var newFreeTextures = Data_Array.filter(function (h) {
                return notEq(h)(matchingTexture.value0);
            })(v.freeTextures);
            var newAcquired = insert(handleId(unwrapTextureHandle(matchingTexture.value0)))(v.acquired);
            return {
                handle: matchingTexture.value0,
                pool: {
                    totalAllocated: v.totalAllocated,
                    totalReleased: v.totalReleased,
                    freeTextures: newFreeTextures,
                    acquired: newAcquired,
                    totalAcquired: v.totalAcquired + 1 | 0
                }
            };
        };
        if (matchingTexture instanceof Data_Maybe.Nothing) {
            var contentKey = spec.label + ("-" + (show(spec.width) + ("x" + (show(spec.height) + ("-" + show(v.totalAllocated))))));
            var newHandle = {
                handle: mkResourceHandle(contentKey),
                spec: spec
            };
            var newAcquired = insert(handleId(unwrapTextureHandle(newHandle)))(v.acquired);
            return {
                handle: newHandle,
                pool: {
                    freeTextures: v.freeTextures,
                    totalReleased: v.totalReleased,
                    acquired: newAcquired,
                    totalAllocated: v.totalAllocated + 1 | 0,
                    totalAcquired: v.totalAcquired + 1 | 0
                }
            };
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 759, column 6 - line 791, column 13): " + [ matchingTexture.constructor.name ]);
    };
};
var ordResourceHandle = {
    compare: function (v) {
        return function (v1) {
            return compare2(new Data_Tuple.Tuple(v.id, v.generation))(new Data_Tuple.Tuple(v1.id, v1.generation));
        };
    },
    Eq0: function () {
        return eqResourceHandle;
    }
};
var eqPipelineKey = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var eq10 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Map_Internal.eqMap(eqPipelineKey)(/* #__PURE__ */ Data_Eq.eqRec()(/* #__PURE__ */ Data_Eq.eqRowCons(/* #__PURE__ */ Data_Eq.eqRowCons(/* #__PURE__ */ Data_Eq.eqRowCons(/* #__PURE__ */ Data_Eq.eqRowCons(Data_Eq.eqRowNil)()({
    reflectSymbol: function () {
        return "shaderSource";
    }
})(Data_Eq.eqString))()({
    reflectSymbol: function () {
        return "lastUsedFrame";
    }
})(Data_Eq.eqInt))()({
    reflectSymbol: function () {
        return "handle";
    }
})(eqResourceHandle))()({
    reflectSymbol: function () {
        return "compilationTime";
    }
})(Data_Eq.eqNumber))));
var ordPipelineKey = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqPipelineKey;
    }
};
var delete1 = /* #__PURE__ */ Data_Map_Internal["delete"](ordPipelineKey);
var insert1 = /* #__PURE__ */ Data_Map_Internal.insert(ordPipelineKey);
var lookup = /* #__PURE__ */ Data_Map_Internal.lookup(ordPipelineKey);
var filter = /* #__PURE__ */ Data_Map_Internal.filter(ordPipelineKey);
var pipelineEvictLRU = function (v) {
    var findLRU = function (entries) {
        var pairs = toUnfoldable(entries);
        var sorted = Data_Array.sortBy(function (v1) {
            return function (v2) {
                return compare(v1.value1.lastUsedFrame)(v2.value1.lastUsedFrame);
            };
        })(pairs);
        var v1 = Data_Array.head(sorted);
        if (v1 instanceof Data_Maybe.Nothing) {
            return Data_Maybe.Nothing.value;
        };
        if (v1 instanceof Data_Maybe.Just) {
            return new Data_Maybe.Just(v1.value0.value0);
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 412, column 10 - line 414, column 38): " + [ v1.constructor.name ]);
    };
    var v1 = findLRU(v.entries);
    if (v1 instanceof Data_Maybe.Nothing) {
        return v;
    };
    if (v1 instanceof Data_Maybe.Just) {
        return {
            maxEntries: v.maxEntries,
            currentFrame: v.currentFrame,
            hits: v.hits,
            misses: v.misses,
            entries: delete1(v1.value0)(v.entries)
        };
    };
    throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 402, column 3 - line 405, column 66): " + [ v1.constructor.name ]);
};
var pipelineInsert = function (key) {
    return function (entry) {
        return function (v) {
            var cache$prime = (function () {
                var $341 = Data_Map_Internal.size(v.entries) >= v.maxEntries;
                if ($341) {
                    return pipelineEvictLRU(v);
                };
                return v;
            })();
            var updatedEntry = {
                compilationTime: entry.compilationTime,
                handle: entry.handle,
                shaderSource: entry.shaderSource,
                lastUsedFrame: cache$prime.currentFrame
            };
            return {
                maxEntries: cache$prime.maxEntries,
                currentFrame: cache$prime.currentFrame,
                hits: cache$prime.hits,
                misses: cache$prime.misses,
                entries: insert1(key)(updatedEntry)(cache$prime.entries)
            };
        };
    };
};
var pipelineLookup = function (key) {
    return function (v) {
        var v1 = lookup(key)(v.entries);
        if (v1 instanceof Data_Maybe.Nothing) {
            return {
                entry: Data_Maybe.Nothing.value,
                cache: {
                    entries: v.entries,
                    maxEntries: v.maxEntries,
                    currentFrame: v.currentFrame,
                    hits: v.hits,
                    misses: v.misses + 1 | 0
                }
            };
        };
        if (v1 instanceof Data_Maybe.Just) {
            var updatedEntry = {
                compilationTime: v1.value0.compilationTime,
                handle: v1.value0.handle,
                shaderSource: v1.value0.shaderSource,
                lastUsedFrame: v.currentFrame
            };
            var updatedEntries = insert1(key)(updatedEntry)(v.entries);
            return {
                entry: new Data_Maybe.Just(updatedEntry),
                cache: {
                    maxEntries: v.maxEntries,
                    currentFrame: v.currentFrame,
                    misses: v.misses,
                    entries: updatedEntries,
                    hits: v.hits + 1 | 0
                }
            };
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 371, column 3 - line 382, column 10): " + [ v1.constructor.name ]);
    };
};
var eqPipelineCache = {
    eq: function (x) {
        return function (y) {
            return x.currentFrame === y.currentFrame && eq10(x.entries)(y.entries) && x.hits === y.hits && x.maxEntries === y.maxEntries && x.misses === y.misses;
        };
    }
};
var eq11 = /* #__PURE__ */ Data_Eq.eq(eqPipelineCache);
var eqBufferBucket = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Bucket1KB && y instanceof Bucket1KB) {
                return true;
            };
            if (x instanceof Bucket4KB && y instanceof Bucket4KB) {
                return true;
            };
            if (x instanceof Bucket16KB && y instanceof Bucket16KB) {
                return true;
            };
            if (x instanceof Bucket64KB && y instanceof Bucket64KB) {
                return true;
            };
            if (x instanceof Bucket256KB && y instanceof Bucket256KB) {
                return true;
            };
            if (x instanceof Bucket1MB && y instanceof Bucket1MB) {
                return true;
            };
            if (x instanceof Bucket4MB && y instanceof Bucket4MB) {
                return true;
            };
            if (x instanceof Bucket16MB && y instanceof Bucket16MB) {
                return true;
            };
            if (x instanceof BucketCustom && y instanceof BucketCustom) {
                return x.value0 === y.value0;
            };
            return false;
        };
    }
};
var eq12 = /* #__PURE__ */ Data_Eq.eq(eqBufferBucket);
var eqBufferHandle = {
    eq: function (x) {
        return function (y) {
            return x.actualSize === y.actualSize && eq12(x.bucket)(y.bucket) && eq7(x.handle)(y.handle);
        };
    }
};
var eq13 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Map_Internal.eqMap(eqBufferBucket)(/* #__PURE__ */ Data_Eq.eqArray(eqBufferHandle)));
var eqBufferPool = {
    eq: function (x) {
        return function (y) {
            return eq6(x.acquired)(y.acquired) && eq13(x.freeLists)(y.freeLists) && x.totalAcquired === y.totalAcquired && x.totalAllocated === y.totalAllocated && x.totalReleased === y.totalReleased;
        };
    }
};
var eq14 = /* #__PURE__ */ Data_Eq.eq(eqBufferPool);
var eqResourceRegistry = {
    eq: function (x) {
        return function (y) {
            return eq14(x.buffers)(y.buffers) && x.currentFrame === y.currentFrame && x.gcThreshold === y.gcThreshold && eq11(x.pipelines)(y.pipelines) && eq9(x.textures)(y.textures);
        };
    }
};
var defaultTexturePool = mkTexturePool;
var defaultPipelineCache = /* #__PURE__ */ mkPipelineCache(256);
var defaultBufferPool = mkBufferPool;
var mkResourceRegistry = {
    pipelines: defaultPipelineCache,
    buffers: defaultBufferPool,
    textures: defaultTexturePool,
    currentFrame: 0,
    gcThreshold: 60
};
var defaultResourceRegistry = mkResourceRegistry;
var collectGarbage = function (v) {
    var threshold = v.currentFrame - v.gcThreshold | 0;
    var freshEntries = filter(function (entry) {
        return entry.lastUsedFrame >= threshold;
    })(v.pipelines.entries);
    var newPipelines = {
        maxEntries: v.pipelines.maxEntries,
        currentFrame: v.pipelines.currentFrame,
        hits: v.pipelines.hits,
        misses: v.pipelines.misses,
        entries: freshEntries
    };
    return {
        buffers: v.buffers,
        textures: v.textures,
        currentFrame: v.currentFrame,
        gcThreshold: v.gcThreshold,
        pipelines: newPipelines
    };
};
var bufferPoolStats = function (v) {
    var totalReused = v.totalAcquired - v.totalAllocated | 0;
    var reuseRate = (function () {
        var $362 = v.totalAcquired > 0;
        if ($362) {
            return Data_Int.toNumber(totalReused) / Data_Int.toNumber(v.totalAcquired);
        };
        return 0.0;
    })();
    var freeCount = foldl1(function (acc) {
        return function (arr) {
            return acc + Data_Array.length(arr) | 0;
        };
    })(0)(Data_Map_Internal.values(v.freeLists));
    var acquiredCount = Data_Set.size(v.acquired);
    return {
        freeCount: freeCount,
        acquiredCount: acquiredCount,
        totalAllocated: v.totalAllocated,
        reuseRate: reuseRate
    };
};
var bufferCountByBucket = function (v) {
    return map(function (v1) {
        return {
            bucket: v1.value0,
            count: Data_Array.length(v1.value1)
        };
    })(toUnfoldable(v.freeLists));
};
var bucketSize = function (v) {
    if (v instanceof Bucket1KB) {
        return 1024;
    };
    if (v instanceof Bucket4KB) {
        return 4096;
    };
    if (v instanceof Bucket16KB) {
        return 16384;
    };
    if (v instanceof Bucket64KB) {
        return 65536;
    };
    if (v instanceof Bucket256KB) {
        return 262144;
    };
    if (v instanceof Bucket1MB) {
        return 1048576;
    };
    if (v instanceof Bucket4MB) {
        return 4194304;
    };
    if (v instanceof Bucket16MB) {
        return 16777216;
    };
    if (v instanceof BucketCustom) {
        return v.value0;
    };
    throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 484, column 1 - line 484, column 34): " + [ v.constructor.name ]);
};
var bufferPoolMemoryUsed = function (v) {
    var averageBucketSize = bucketSize(Bucket64KB.value);
    return v.totalAllocated * averageBucketSize | 0;
};
var bufferPoolUnderThreshold = function (thresholdBytes) {
    return function (pool) {
        return bufferPoolMemoryUsed(pool) < thresholdBytes;
    };
};
var registryMemoryEstimate = function (v) {
    return bufferPoolMemoryUsed(v.buffers) + texturePoolMemoryUsed(v.textures) | 0;
};
var registryStats = function (v) {
    return {
        pipelineStats: pipelineCacheStats(v.pipelines),
        bufferStats: bufferPoolStats(v.buffers),
        textureStats: texturePoolStats(v.textures),
        totalMemoryBytes: registryMemoryEstimate(v),
        currentFrame: v.currentFrame
    };
};
var registryUnderBudget = function (budgetBytes) {
    return function (reg) {
        return registryMemoryEstimate(reg) < budgetBytes;
    };
};
var ordBufferBucket = {
    compare: function (a) {
        return function (b) {
            return compare(bucketSize(a))(bucketSize(b));
        };
    },
    Eq0: function () {
        return eqBufferBucket;
    }
};
var lookup1 = /* #__PURE__ */ Data_Map_Internal.lookup(ordBufferBucket);
var insert2 = /* #__PURE__ */ Data_Map_Internal.insert(ordBufferBucket);
var bufferRelease = function (v) {
    return function (v1) {
        var resourceId = handleId(v.handle);
        var newAcquired = $$delete(resourceId)(v1.acquired);
        var currentList = (function () {
            var v2 = lookup1(v.bucket)(v1.freeLists);
            if (v2 instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            if (v2 instanceof Data_Maybe.Just) {
                return v2.value0;
            };
            throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 626, column 21 - line 628, column 38): " + [ v2.constructor.name ]);
        })();
        var newFreeList = Data_Array.cons(v)(currentList);
        var newFreeLists = insert2(v.bucket)(newFreeList)(v1.freeLists);
        return {
            totalAllocated: v1.totalAllocated,
            totalAcquired: v1.totalAcquired,
            freeLists: newFreeLists,
            acquired: newAcquired,
            totalReleased: v1.totalReleased + 1 | 0
        };
    };
};
var bucketForSize = function (size) {
    if (size <= 1024) {
        return Bucket1KB.value;
    };
    if (size <= 4096) {
        return Bucket4KB.value;
    };
    if (size <= 16384) {
        return Bucket16KB.value;
    };
    if (size <= 65536) {
        return Bucket64KB.value;
    };
    if (size <= 262144) {
        return Bucket256KB.value;
    };
    if (size <= 1048576) {
        return Bucket1MB.value;
    };
    if (size <= 4194304) {
        return Bucket4MB.value;
    };
    if (size <= 16777216) {
        return Bucket16MB.value;
    };
    if (Data_Boolean.otherwise) {
        return new BucketCustom(size);
    };
    throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 496, column 1 - line 496, column 37): " + [ size.constructor.name ]);
};
var bufferAcquire = function (spec) {
    return function (v) {
        var unwrapBufferHandle = function (v1) {
            return v1.handle;
        };
        var bucket = bucketForSize(spec.size);
        var freeList = lookup1(bucket)(v.freeLists);
        var v1 = bind(freeList)(Data_Array.head);
        if (v1 instanceof Data_Maybe.Just) {
            var newFreeList = (function () {
                if (freeList instanceof Data_Maybe.Nothing) {
                    return [  ];
                };
                if (freeList instanceof Data_Maybe.Just) {
                    return Data_Array.drop(1)(freeList.value0);
                };
                throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 587, column 28 - line 589, column 44): " + [ freeList.constructor.name ]);
            })();
            var newFreeLists = insert2(bucket)(newFreeList)(v.freeLists);
            var newAcquired = insert(handleId(unwrapBufferHandle(v1.value0)))(v.acquired);
            return {
                handle: v1.value0,
                pool: {
                    totalAllocated: v.totalAllocated,
                    totalReleased: v.totalReleased,
                    freeLists: newFreeLists,
                    acquired: newAcquired,
                    totalAcquired: v.totalAcquired + 1 | 0
                }
            };
        };
        if (v1 instanceof Data_Maybe.Nothing) {
            var contentKey = spec.label + ("-" + (show(spec.size) + ("-" + show(v.totalAllocated))));
            var newHandle = {
                handle: mkResourceHandle(contentKey),
                bucket: bucket,
                actualSize: bucketSize(bucket)
            };
            var newAcquired = insert(handleId(unwrapBufferHandle(newHandle)))(v.acquired);
            return {
                handle: newHandle,
                pool: {
                    freeLists: v.freeLists,
                    totalReleased: v.totalReleased,
                    acquired: newAcquired,
                    totalAllocated: v.totalAllocated + 1 | 0,
                    totalAcquired: v.totalAcquired + 1 | 0
                }
            };
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Resource (line 584, column 6 - line 616, column 13): " + [ v1.constructor.name ]);
    };
};
var estimateBufferMemory = function (spec) {
    return bucketSize(bucketForSize(spec.size));
};
var allFreeBuffers = function (v) {
    return foldl1(function (acc) {
        return function (arr) {
            return append1(acc)(arr);
        };
    })([  ])(Data_Map_Internal.values(v.freeLists));
};
var findSmallestFittingBuffer = function (requestedSize) {
    return function (pool) {
        var allFree = allFreeBuffers(pool);
        var fitting = Data_Array.filter(function (v) {
            return v.actualSize >= requestedSize;
        })(allFree);
        var sorted = Data_Array.sortBy(function (v) {
            return function (v1) {
                return compare(v.actualSize)(v1.actualSize);
            };
        })(fitting);
        return Data_Array.head(sorted);
    };
};
var largeBuffers = function (thresholdBytes) {
    return function (pool) {
        return Data_Array.filter(function (v) {
            return v.actualSize > thresholdBytes;
        })(allFreeBuffers(pool));
    };
};
var advanceFrame = function (v) {
    var newPipelines = {
        entries: v.pipelines.entries,
        maxEntries: v.pipelines.maxEntries,
        hits: v.pipelines.hits,
        misses: v.pipelines.misses,
        currentFrame: v.currentFrame + 1 | 0
    };
    return {
        buffers: v.buffers,
        textures: v.textures,
        gcThreshold: v.gcThreshold,
        currentFrame: v.currentFrame + 1 | 0,
        pipelines: newPipelines
    };
};
export {
    ResourceHandle,
    handleId,
    handleGeneration,
    mkResourceHandle,
    incrementGeneration,
    ResourceId,
    mkResourceId,
    resourceIdToString,
    mkPipelineCache,
    mkPipelineKey,
    pipelineLookup,
    pipelineInsert,
    pipelineEvictLRU,
    pipelineCacheSize,
    pipelineCacheStats,
    mkBufferPool,
    mkBufferSpec,
    bufferAcquire,
    bufferRelease,
    bufferPoolStats,
    mkTexturePool,
    mkTextureSpec,
    textureAcquire,
    textureRelease,
    texturePoolStats,
    mkResourceRegistry,
    registryPipelines,
    registryBuffers,
    registryTextures,
    registryFrame,
    advanceFrame,
    collectGarbage,
    Bucket1KB,
    Bucket4KB,
    Bucket16KB,
    Bucket64KB,
    Bucket256KB,
    Bucket1MB,
    Bucket4MB,
    Bucket16MB,
    BucketCustom,
    bucketSize,
    bucketForSize,
    Dim2D,
    Dim3D,
    DimCube,
    defaultPipelineCache,
    defaultBufferPool,
    defaultTexturePool,
    defaultResourceRegistry,
    estimateBufferMemory,
    estimateTextureMemory,
    bufferPoolMemoryUsed,
    texturePoolMemoryUsed,
    mapFreeBuffers,
    mapFreeTextures,
    pipelineKeys,
    allFreeBuffers,
    bufferCountByBucket,
    recentPipelines,
    expensivePipelines,
    largeBuffers,
    largeTextures,
    withPipelines,
    withBuffers,
    withTextures,
    registryMemoryEstimate,
    registryStats,
    pipelineHasCapacity,
    bufferPoolUnderThreshold,
    texturePoolUnderThreshold,
    registryUnderBudget,
    findSmallestFittingBuffer,
    findSmallestFittingTexture,
    eqResourceId,
    ordResourceId,
    showResourceId,
    eqResourceHandle,
    ordResourceHandle,
    showResourceHandle,
    eqPipelineKey,
    ordPipelineKey,
    showPipelineKey,
    eqPipelineCache,
    showPipelineCache,
    eqBufferBucket,
    ordBufferBucket,
    showBufferBucket,
    eqBufferHandle,
    showBufferHandle,
    eqBufferPool,
    showBufferPool,
    eqTextureDimension,
    ordTextureDimension,
    showTextureDimension,
    eqTextureHandle,
    showTextureHandle,
    eqTexturePool,
    showTexturePool,
    eqResourceRegistry,
    showResourceRegistry
};
//# sourceMappingURL=index.js.map
