// Generated by purs version 0.15.15
import * as Data_Generic_Rep from "../Data.Generic.Rep/index.js";
import * as Data_Show_Generic from "../Data.Show.Generic/index.js";
var genericShowConstructor = /* #__PURE__ */ Data_Show_Generic.genericShowConstructor(Data_Show_Generic.genericShowArgsNoArguments);
var SMRealtime = /* #__PURE__ */ (function () {
    function SMRealtime() {

    };
    SMRealtime.value = new SMRealtime();
    return SMRealtime;
})();
var SMStepBased = /* #__PURE__ */ (function () {
    function SMStepBased() {

    };
    SMStepBased.value = new SMStepBased();
    return SMStepBased;
})();
var SMSubstepped = /* #__PURE__ */ (function () {
    function SMSubstepped() {

    };
    SMSubstepped.value = new SMSubstepped();
    return SMSubstepped;
})();
var RBWebGL2 = /* #__PURE__ */ (function () {
    function RBWebGL2() {

    };
    RBWebGL2.value = new RBWebGL2();
    return RBWebGL2;
})();
var RBWebGPU = /* #__PURE__ */ (function () {
    function RBWebGPU() {

    };
    RBWebGPU.value = new RBWebGPU();
    return RBWebGPU;
})();
var RBCPUFallback = /* #__PURE__ */ (function () {
    function RBCPUFallback() {

    };
    RBCPUFallback.value = new RBCPUFallback();
    return RBCPUFallback;
})();
var genericSimulationMode_ = {
    to: function (x) {
        if (x instanceof Data_Generic_Rep.Inl) {
            return SMRealtime.value;
        };
        if (x instanceof Data_Generic_Rep.Inr && x.value0 instanceof Data_Generic_Rep.Inl) {
            return SMStepBased.value;
        };
        if (x instanceof Data_Generic_Rep.Inr && x.value0 instanceof Data_Generic_Rep.Inr) {
            return SMSubstepped.value;
        };
        throw new Error("Failed pattern match at Lattice.State.ParticleConfig (line 40, column 1 - line 40, column 41): " + [ x.constructor.name ]);
    },
    from: function (x) {
        if (x instanceof SMRealtime) {
            return new Data_Generic_Rep.Inl(Data_Generic_Rep.NoArguments.value);
        };
        if (x instanceof SMStepBased) {
            return new Data_Generic_Rep.Inr(new Data_Generic_Rep.Inl(Data_Generic_Rep.NoArguments.value));
        };
        if (x instanceof SMSubstepped) {
            return new Data_Generic_Rep.Inr(new Data_Generic_Rep.Inr(Data_Generic_Rep.NoArguments.value));
        };
        throw new Error("Failed pattern match at Lattice.State.ParticleConfig (line 40, column 1 - line 40, column 41): " + [ x.constructor.name ]);
    }
};
var showSimulationMode = {
    show: /* #__PURE__ */ Data_Show_Generic.genericShow(genericSimulationMode_)(/* #__PURE__ */ Data_Show_Generic.genericShowSum(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "SMRealtime";
        }
    }))(/* #__PURE__ */ Data_Show_Generic.genericShowSum(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "SMStepBased";
        }
    }))(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "SMSubstepped";
        }
    }))))
};
var genericRenderingBackend_ = {
    to: function (x) {
        if (x instanceof Data_Generic_Rep.Inl) {
            return RBWebGL2.value;
        };
        if (x instanceof Data_Generic_Rep.Inr && x.value0 instanceof Data_Generic_Rep.Inl) {
            return RBWebGPU.value;
        };
        if (x instanceof Data_Generic_Rep.Inr && x.value0 instanceof Data_Generic_Rep.Inr) {
            return RBCPUFallback.value;
        };
        throw new Error("Failed pattern match at Lattice.State.ParticleConfig (line 33, column 1 - line 33, column 43): " + [ x.constructor.name ]);
    },
    from: function (x) {
        if (x instanceof RBWebGL2) {
            return new Data_Generic_Rep.Inl(Data_Generic_Rep.NoArguments.value);
        };
        if (x instanceof RBWebGPU) {
            return new Data_Generic_Rep.Inr(new Data_Generic_Rep.Inl(Data_Generic_Rep.NoArguments.value));
        };
        if (x instanceof RBCPUFallback) {
            return new Data_Generic_Rep.Inr(new Data_Generic_Rep.Inr(Data_Generic_Rep.NoArguments.value));
        };
        throw new Error("Failed pattern match at Lattice.State.ParticleConfig (line 33, column 1 - line 33, column 43): " + [ x.constructor.name ]);
    }
};
var showRenderingBackend = {
    show: /* #__PURE__ */ Data_Show_Generic.genericShow(genericRenderingBackend_)(/* #__PURE__ */ Data_Show_Generic.genericShowSum(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "RBWebGL2";
        }
    }))(/* #__PURE__ */ Data_Show_Generic.genericShowSum(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "RBWebGPU";
        }
    }))(/* #__PURE__ */ genericShowConstructor({
        reflectSymbol: function () {
            return "RBCPUFallback";
        }
    }))))
};
var eqSimulationMode = {
    eq: function (x) {
        return function (y) {
            if (x instanceof SMRealtime && y instanceof SMRealtime) {
                return true;
            };
            if (x instanceof SMStepBased && y instanceof SMStepBased) {
                return true;
            };
            if (x instanceof SMSubstepped && y instanceof SMSubstepped) {
                return true;
            };
            return false;
        };
    }
};
var eqRenderingBackend = {
    eq: function (x) {
        return function (y) {
            if (x instanceof RBWebGL2 && y instanceof RBWebGL2) {
                return true;
            };
            if (x instanceof RBWebGPU && y instanceof RBWebGPU) {
                return true;
            };
            if (x instanceof RBCPUFallback && y instanceof RBCPUFallback) {
                return true;
            };
            return false;
        };
    }
};
var updateSimulationMode = function (m) {
    return function (prefs) {
        return {
            maxParticles: prefs.maxParticles,
            targetFps: prefs.targetFps,
            checkpointInterval: prefs.checkpointInterval,
            backend: prefs.backend,
            enableTrails: prefs.enableTrails,
            enableCollisions: prefs.enableCollisions,
            qualityMultiplier: prefs.qualityMultiplier,
            simulationMode: m
        };
    };
};
var updateBackend = function (b) {
    return function (prefs) {
        return {
            maxParticles: prefs.maxParticles,
            targetFps: prefs.targetFps,
            checkpointInterval: prefs.checkpointInterval,
            simulationMode: prefs.simulationMode,
            enableTrails: prefs.enableTrails,
            enableCollisions: prefs.enableCollisions,
            qualityMultiplier: prefs.qualityMultiplier,
            backend: b
        };
    };
};
var lowEndPreset = /* #__PURE__ */ (function () {
    return {
        maxParticles: 1000,
        targetFps: 30,
        checkpointInterval: 600,
        backend: RBCPUFallback.value,
        simulationMode: SMStepBased.value,
        enableTrails: false,
        enableCollisions: false,
        qualityMultiplier: 0.5
    };
})();
var isValidTargetFps = function (n) {
    return n > 0 && n <= 240;
};
var updateTargetFps = function (n) {
    return function (prefs) {
        var $64 = isValidTargetFps(n);
        if ($64) {
            return {
                maxParticles: prefs.maxParticles,
                checkpointInterval: prefs.checkpointInterval,
                backend: prefs.backend,
                simulationMode: prefs.simulationMode,
                enableTrails: prefs.enableTrails,
                enableCollisions: prefs.enableCollisions,
                qualityMultiplier: prefs.qualityMultiplier,
                targetFps: n
            };
        };
        return prefs;
    };
};
var isValidMaxParticles = function (n) {
    return n > 0;
};
var updateMaxParticles = function (n) {
    return function (prefs) {
        var $65 = isValidMaxParticles(n);
        if ($65) {
            return {
                targetFps: prefs.targetFps,
                checkpointInterval: prefs.checkpointInterval,
                backend: prefs.backend,
                simulationMode: prefs.simulationMode,
                enableTrails: prefs.enableTrails,
                enableCollisions: prefs.enableCollisions,
                qualityMultiplier: prefs.qualityMultiplier,
                maxParticles: n
            };
        };
        return prefs;
    };
};
var isValidCheckpointInterval = function (n) {
    return n > 0;
};
var updateCheckpointInterval = function (n) {
    return function (prefs) {
        var $66 = isValidCheckpointInterval(n);
        if ($66) {
            return {
                maxParticles: prefs.maxParticles,
                targetFps: prefs.targetFps,
                backend: prefs.backend,
                simulationMode: prefs.simulationMode,
                enableTrails: prefs.enableTrails,
                enableCollisions: prefs.enableCollisions,
                qualityMultiplier: prefs.qualityMultiplier,
                checkpointInterval: n
            };
        };
        return prefs;
    };
};
var highEndPreset = /* #__PURE__ */ (function () {
    return {
        maxParticles: 100000,
        targetFps: 120,
        checkpointInterval: 150,
        backend: RBWebGPU.value,
        simulationMode: SMSubstepped.value,
        enableTrails: true,
        enableCollisions: true,
        qualityMultiplier: 2.0
    };
})();
var defaultPreferences = /* #__PURE__ */ (function () {
    return {
        maxParticles: 10000,
        targetFps: 60,
        checkpointInterval: 300,
        backend: RBWebGL2.value,
        simulationMode: SMRealtime.value,
        enableTrails: false,
        enableCollisions: false,
        qualityMultiplier: 1.0
    };
})();
var backendDescription = function (v) {
    if (v instanceof RBWebGL2) {
        return "WebGL 2.0 (GPU-accelerated)";
    };
    if (v instanceof RBWebGPU) {
        return "WebGPU (next-gen GPU)";
    };
    if (v instanceof RBCPUFallback) {
        return "CPU Fallback (compatibility)";
    };
    throw new Error("Failed pattern match at Lattice.State.ParticleConfig (line 136, column 1 - line 136, column 49): " + [ v.constructor.name ]);
};
export {
    RBWebGL2,
    RBWebGPU,
    RBCPUFallback,
    SMRealtime,
    SMStepBased,
    SMSubstepped,
    defaultPreferences,
    lowEndPreset,
    highEndPreset,
    updateMaxParticles,
    updateTargetFps,
    updateCheckpointInterval,
    updateBackend,
    updateSimulationMode,
    isValidMaxParticles,
    isValidTargetFps,
    isValidCheckpointInterval,
    backendDescription,
    eqRenderingBackend,
    genericRenderingBackend_,
    showRenderingBackend,
    eqSimulationMode,
    genericSimulationMode_,
    showSimulationMode
};
//# sourceMappingURL=index.js.map
